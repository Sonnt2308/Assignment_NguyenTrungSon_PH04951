<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.assignment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ScreenView.ScreenView.UpdateClass"></activity>
        <activity android:name=".ScreenView.ScreenView.Dialog_Activity1" />

        <activity android:name=".ScreenView.ScreenView.quanlisinhvien" />
        <activity android:name=".ScreenView.ScreenView.danhsachlop" />
        <activity android:name=".ScreenView.ScreenView.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>